# Tip: Install direnv to automatically run this script.
# direnv is an extension for your shell. It augments
# existing shells with a new feature that can load and
# unload environment variables depending on the current
# directory.

export PATH=$PATH:$(pwd)/scripts
docker compose --env-file ./.env up -d
